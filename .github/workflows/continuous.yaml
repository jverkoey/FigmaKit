name: Swift

on: [push]

jobs:
  build:

    runs-on: macos-latest

    steps:
      - uses: fwal/setup-swift@v1
      - name: Get swift version
        run: swift --version
      - uses: actions/checkout@v2
      - name: Build
        run: swift build
      - name: Run tests
        run: swift test